# Layer Drop Folder

Put future separated artwork here.

Expected names for the example layered profile:

- `room.png`
- `samurai.png`
- `foreground.png`

The example manifest keeps `samurai.png` optional while artwork is in progress, but `room.png` and `foreground.png` are required before the layered profile can be safely activated.
